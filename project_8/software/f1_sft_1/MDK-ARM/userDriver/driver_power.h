/* [新增] driver_power.h — 低功耗管理驱动，框架原无此模块，本次新增 */
#ifndef __DRIVER_POWER_H__
#define __DRIVER_POWER_H__

/*============================================================================
 * 向下依赖（driver层向HAL层索要）
 * 依赖：HAL_PWR — HAL_PWR_EnterSLEEPMode（STM32 HAL低功耗接口）
 *============================================================================*/

/*============================================================================
 * 向上提供（API）
 *============================================================================*/

/* 进入低功耗SLEEP模式（主调压器开启，WFI唤醒），等待中断后返回 */
void DRIVER_POWER_EnterSleep(void);

/* 唤醒后恢复外设（当前为空，外设由task层在sleepFlag清零时处理） */
void DRIVER_POWER_WakeUp(void);

#endif
